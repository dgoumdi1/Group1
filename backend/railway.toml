[build]
builder = "NIXPACKS"
buildCommand = "./gradlew :app:bootJar"

[deploy]
startCommand = "sh -c 'java -jar app/build/libs/*.jar --server.port=${PORT:-8090}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
